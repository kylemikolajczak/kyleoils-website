<div class="trust-bar" aria-label="Vertrauen und Qualifikation">
  <slot />
</div>

<style>
  .trust-bar { display: grid; gap: 1px; overflow: hidden; border: 1px solid rgb(216 199 173 / 70%); border-radius: var(--radius-card); background: var(--color-sand); }
  .trust-bar :global(p) { margin: 0; padding: var(--space-20); background: var(--color-warm-white); color: var(--color-text-secondary); font-size: var(--font-size-small); font-weight: var(--font-weight-medium); text-align: center; }
  @media (min-width: 768px) { .trust-bar { grid-template-columns: repeat(3, 1fr); } }
</style>
