---
import SiteHeader from '../components/navigation/SiteHeader.astro';
import SiteFooter from '../components/global/SiteFooter.astro';
import { getLanguagePaths, type Locale } from '../data/locales';
import tokensCss from '../styles/tokens.css?inline';
import globalCss from '../styles/global.css?inline';

interface Props {
  title: string;
  description?: string;
  lang?: Locale;
}

const { title, description, lang = 'de' } = Astro.props;
const currentPath = Astro.url.pathname;
const languagePaths = getLanguagePaths(currentPath);
const baseUrl = Astro.site ?? Astro.url.origin;
const canonicalUrl = new URL(currentPath, baseUrl);
const germanUrl = languagePaths ? new URL(languagePaths.de, baseUrl) : undefined;
const englishUrl = languagePaths ? new URL(languagePaths.en, baseUrl) : undefined;
const socialImageUrl = new URL('/og-image.jpg', baseUrl);
const siteUrl = 'https://kyleoils.com';
const personId = `${siteUrl}/#person`;
const organizationId = `${siteUrl}/#organization`;
const structuredData = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'WebSite',
      '@id': `${siteUrl}/#website`,
      name: 'Kyle Oils',
      url: siteUrl,
      inLanguage: ['de', 'en'],
      publisher: {
        '@id': organizationId,
      },
    },
    {
      '@type': 'Person',
      '@id': personId,
      name: 'Kyle Daniel Mikolajczak',
      jobTitle: 'doTERRA Wellness Advocate',
      url: siteUrl,
      logo: `${siteUrl}/favicon-32x32.png`,
      image: `${siteUrl}/assets/images/about/kyle-about-hero.jpg`,
      description:
        'Persönliche Beratung zu ätherischen Ölen, natürlichen Routinen und doTERRA-Produkten.',
    },
    {
      '@type': 'Organization',
      '@id': organizationId,
      name: 'Kyle Oils',
      url: siteUrl,
      logo: `${siteUrl}/favicon-32x32.png`,
    },
  ],
};
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <style set:html={tokensCss} />
    <style set:html={globalCss} />
    <meta name="theme-color" content="#a8b8a0" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    <link rel="canonical" href={canonicalUrl} />
    {germanUrl && <link rel="alternate" hreflang="de" href={germanUrl} />}
    {englishUrl && <link rel="alternate" hreflang="en" href={englishUrl} />}
    {germanUrl && <link rel="alternate" hreflang="x-default" href={germanUrl} />}
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Kyle Daniel Mikolajczak" />
    <meta property="og:title" content={title} />
    {description && <meta property="og:description" content={description} />}
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={socialImageUrl} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    {description && <meta name="twitter:description" content={description} />}
    <meta name="twitter:image" content={socialImageUrl} />
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  </head>
  <body>
    <a class="skip-link" href="#main-content">{lang === 'de' ? 'Zum Inhalt springen' : 'Skip to content'}</a>
    <SiteHeader locale={lang} {currentPath} />
    <main id="main-content"><slot /></main>
    <SiteFooter locale={lang} />
  </body>
</html>
