---
import BaseLayout from './BaseLayout.astro';
import type { Locale } from '../data/locales';

interface Props {
  title: string;
  description?: string;
  lang?: Locale;
  noindex?: boolean;
}

const { title, description, lang = 'de', noindex = false } = Astro.props;
---

<BaseLayout title={title} description={description} {lang} {noindex}>
  <div class="page-shell"><slot /></div>
</BaseLayout>

<style>
  .page-shell { min-height: 50vh; }
</style>
